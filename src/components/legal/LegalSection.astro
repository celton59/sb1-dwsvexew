---
interface Props {
  title: string;
  id?: string;
}

const { title, id } = Astro.props;
---

<section class="mb-8" id={id}>
  <h2 class="text-2xl font-semibold mb-4 text-gray-900">{title}</h2>
  <div class="space-y-4">
    <slot />
  </div>
</section>